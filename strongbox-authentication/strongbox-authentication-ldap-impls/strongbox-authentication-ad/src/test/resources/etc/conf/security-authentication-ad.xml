<ldap-configuration>
    
    <host>localhost</host>
    <port>40636</port>
    <protocol>ldaps</protocol>
    <username>admin</username>
    <password>password</password>
    <!-- TODO: Change this to "principal" when testing against a real Active Directory server -->
    <loginMode>anonymous</loginMode>
    <domain>carlspring.com</domain>
    <timeout>30000</timeout>
    <rootDn>dc=carlspring,dc=com</rootDn>
    <!-- Supports: "simple", "none", "DIGEST-MD5", "CRAM-MD5" -->
    <authenticationType>simple</authenticationType>

    <attribute-mappings>
        <user-mapping>
            <uid>sAMAccountName</uid>
            <password>userPassword</password>
            <fullName>displayName</fullName>
            <email>mail</email>

            <filter></filter>

            <query>
                <objectClass>user</objectClass>
                <baseDn>dc=carlspring,dc=com</baseDn>
            </query>
        </user-mapping>
        <group-mapping>
            <groupId>cn</groupId>
            <groupMember>uniqueMember</groupMember>

            <query>
                <objectClass>groupOfUniqueNames</objectClass>
                <baseDn>ou=groups</baseDn>
            </query>
        </group-mapping>
    </attribute-mappings>
    
</ldap-configuration>